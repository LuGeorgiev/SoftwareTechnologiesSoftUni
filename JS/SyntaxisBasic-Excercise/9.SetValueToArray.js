/*jshint esversion: 6 */
function solve(args) {
    const length = +args[0];
    const arr = [];
    for (let index = 0; index < length; index++) {
        arr.push(0);
    }

    for (let index = 1; index < args.length; index++) {
        const element = array[index];

    }
}
solve('6');